# Elevate AI — frontend

Vite + React + TypeScript client for the local voice tutor.

```powershell
npm install
npm run dev
```

Opens [http://localhost:5173](http://localhost:5173). WebSocket `/ws` is proxied to FastAPI on port 8000.

See the repo [README](../README.md) for the full stack setup.
